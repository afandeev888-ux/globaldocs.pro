# ========================================
# РЕДИРЕКТЫ HTTP → HTTPS и WWW → БЕЗ WWW
# ========================================
RewriteEngine On

# Универсальный редирект: www → без www И http → https
# Работает для всех вариантов: http://www, https://www, http://безwww
RewriteCond %{HTTP_HOST} ^www\. [NC,OR]
RewriteCond %{HTTPS} off
RewriteCond %{HTTP_HOST} ^(?:www\.)?(.+)$ [NC]
RewriteRule ^(.*)$ https://%1/$1 [L,R=301]

# HSTS - форсирование HTTPS в браузере (1 год)
Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"

# Безопасность
Header always set X-Content-Type-Options "nosniff"
Header always set X-Frame-Options "SAMEORIGIN"
Header always set X-XSS-Protection "1; mode=block"

# Кэширование статики (разумные сроки)
<IfModule mod_expires.c>
  ExpiresActive On
  # Картинки - 1 месяц
  ExpiresByType image/jpeg "access plus 1 month"
  ExpiresByType image/png "access plus 1 month"
  ExpiresByType image/gif "access plus 1 month"
  ExpiresByType image/x-icon "access plus 1 month"
  ExpiresByType image/webp "access plus 1 month"
  # CSS/JS - 1 неделя
  ExpiresByType text/css "access plus 1 week"
  ExpiresByType application/javascript "access plus 1 week"
  # Шрифты - 1 год (почти не меняются)
  ExpiresByType font/woff "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"
  ExpiresByType application/font-woff "access plus 1 year"
  ExpiresByType application/font-woff2 "access plus 1 year"
  # HTML - не кэшировать
  ExpiresByType text/html "access plus 0 seconds"
</IfModule>

# Сжатие GZIP
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css application/javascript application/json
</IfModule>
